
{cover_enabled, true}.
{deps, [
    %% TODO: bind to a specific commit or tag instead of 'master'
    {proper, ".*", {git, "http://github.com/jrobhoward/proper.git", {branch, "SCADAbase"}}}
]}.

{plugin_dir, "priv/build/plugins"}.
{plugins, [eqc_resolver]}.
